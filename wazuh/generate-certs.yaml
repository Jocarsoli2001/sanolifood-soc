name: sanolifood-wazuh-certificates

services:
  generator:
    image: wazuh/wazuh-certs-generator:0.0.4
    environment:
      CERT_TOOL_VERSION: "4.14"
    volumes:
      - ./runtime/certs:/certificates
      - ./config/certs.yml:/config/certs.yml:ro
