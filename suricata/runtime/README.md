# Suricata runtime

`make suricata-discover` creates `.env` here with the capture interface and
HOME_NET discovered from the Ubuntu host. The generated file is intentionally
ignored because interface names and addresses vary between installations.

No credentials are stored in this directory.
