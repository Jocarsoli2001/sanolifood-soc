# Runtime SOAR

Este directorio se genera localmente mediante `make soar-up`. Contiene
credenciales, estados de activación y secretos compartidos entre Wazuh, n8n y
el controlador. Excepto este archivo y `.gitignore`, su contenido nunca debe
incorporarse a Git ni a las evidencias.
